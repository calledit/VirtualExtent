# files in src

- VirtualExtentLoader/
	Contains a simple executable that loads the injection dll in to itself. (only used for development.)

- VirualExtentDll/
	Contains the virtualExtend dll which is injected to a process so that it can hook varoius functions if needed